export class SpatialMap {
	// private entities: Entity[] = [];
	// public add(entity: Entity): void {
	// 	this.entities.push(entity);
	// }
	// public query(position: Vector2, size: Vector2): Entity[] {
	// 	return this.entities.filter((entity) => {
	// 		return entity.position.x < position.x + size.x && position.x < entity.x + entity.size.x && entity.position.y < position.y + size.y && position.y < entity.y + entity.size.y;
	// 	});
	// }
}
